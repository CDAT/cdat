set(SCIPY_MAJOR 0)
set(SCIPY_MINOR 10)
set(SCIPY_PATCH 0)
set(SCIPY_MAJOR_SRC 0)
set(SCIPY_MINOR_SRC 12)
set(SCIPY_PATCH_SRC 0)
set(SCIPY_URL ${LLNL_URL})
set(SCIPY_GZ scipy-${SCIPY_MAJOR_SRC}.${SCIPY_MINOR_SRC}.${SCIPY_PATCH_SRC}.tar.gz)
set(SCIPY_MD5 8fb4da324649f655e8557ea92b998786 )
set(SCIPY_SOURCE ${SCIPY_URL}/${SCIPY_GZ})

set (nm SCIPY)
string(TOUPPER ${nm} uc_nm)
set(${uc_nm}_VERSION ${${nm}_MAJOR_SRC}.${${nm}_MINOR_SRC}.${${nm}_PATCH_SRC})

add_cdat_package_dependent(SCIPY "" "" ON "CDAT_BUILD_WO_ESGF" OFF)
