from test_python_ok import *
# The main installation script is installation/install.py
# However, we need to first check for problems using 1.5.2 syntax only.

execfile(os.path.join('installation', 'install.py'))
